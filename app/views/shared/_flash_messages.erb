<% unless flash(:flash).empty? %>
  <div id="flash-messages">
    <% flash(:flash).each do |message| %>
      <div class="alert alert-info">
        <button type="button" class="close" data-dismiss="alert">×</button>
        <strong>Notice!</strong> <%= message[1] %>
      </div>
    <% end %>
  </div>
<% end %>
