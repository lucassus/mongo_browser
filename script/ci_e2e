#!/bin/bash

# Start test backend instance
bundle exec thin start -R config.ru -e test -p 4000 -d

# Wait for thin
sleep 1

(cd frontend ; grunt test:e2e)
RESULT=$?

# Kill test backend instance
kill `cat tmp/pids/thin.pid`
exit $RESULT
